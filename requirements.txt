Django >= 3.2, < 3.3 # Django webframework

vobject          # Ical generation
ReportLab        # PDF generation

lxml             # Web scraping of data
sphinx           # To build docs

django-compressor

tqdm
